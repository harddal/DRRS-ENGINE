#include "../entity.asc"

void init(entityid self)
{
	
}

void update(entityid self)
{

}

void onLogicEvent(entityid self)
{
    player::lock(true);
    camera::switchTo(self);
}

void onRevealDone(entityid entityID)
{
    camera::switchToPlayer();
    player::lock(false);
}