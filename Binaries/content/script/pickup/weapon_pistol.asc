#include "../entity.asc"

[export]
int g_ammoAddAmount = 5;

void onPlayerCollide(entityid self)
{
	// TODO: add ammo to shotgun
	sound::playAt(transform::position(self), "content/sound/pickup/shotgun_shell_pile.wav");
	
	entity::kill(self);
}