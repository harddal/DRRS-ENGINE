#include "../entity.asc"

[export]
int g_healthValue = 25;

void onPlayerCollide(entityid self)
{
	if (player::health() < player::max_health())
	{
		player::heal(g_healthValue);
		
		sound::playAt(transform::position(self), "content/sound/pickup/medkit.wav");
		
		entity::kill(self);
	}
}