#include "../entity.asc"

// A single-use upgrade dose. Nothing to gate on yet, so canUse is omitted
// entirely — an item without the hook is always usable.
void onUse(entityid self)
{
	player::heal(player::max_health());
}
