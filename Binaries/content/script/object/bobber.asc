#include "../entity.asc"

[export]
float g_amplitude = 0.25;

[export]
float g_speed = 1.0;

vector3d g_origin;

void init(entityid self)
{
    g_origin = transform::position(self);
}

void update(entityid self)
{
    float t = float(time::get()) * 0.001 * g_speed;

    transform::position(self, vector3d(
        g_origin.x,
        g_origin.y + sin(t) * g_amplitude,
        g_origin.z
    ));
}
