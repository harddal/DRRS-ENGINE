#include "../entity.asc"

[export]
bool g_startVisible = true;

bool g_visible;

void init(entityid self)
{
    g_visible = g_startVisible;
    render::visible(self, g_visible);
}

void onLogicEvent(entityid self)
{
    g_visible = !g_visible;
    render::visible(self, g_visible);
}
